# Git Upload Commands

Run these from the cleaned repository root:

```cmd
cd C:\Users\HP\Desktop\Shield_GNN1
git status
git add .
git commit -m "Initial cleaned research release of SHIELD-GNN"
git remote -v
git push -u origin main
```

Login through GitHub browser/token when prompted. Do not force push.
