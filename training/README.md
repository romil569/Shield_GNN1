# Training Entrypoints

Run canonical commands from the repository root using the `src` package layout. Copies of the main training scripts are included here for discoverability, but the `src` versions are the canonical entrypoints.
